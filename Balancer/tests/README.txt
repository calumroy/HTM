README.txt

# This is a directory containing unit tests for the HTM project
# to run the tests install the requirements in a virtualenv
# Run the tests with the "nosetests" command
# The temporal memory tests can take a fairly long time 5 minutes or more